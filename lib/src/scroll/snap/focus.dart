import 'package:flutter/material.dart';

class ScrollSnapFocus extends StatelessWidget {
  const ScrollSnapFocus({
    super.key,
    required this.child,
  });

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
